{
  "name": "arnob-portfolio",
  "version": "1.0.0",
  "description": "Arnob's Dynamic Portfolio with Admin CRUD Dashboard",
  "type": "module",
  "scripts": {
    "build": "npm run install-all && npm run build --prefix client && node build-vercel.js",
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "start": "npm run build --prefix client && npm start --prefix server",
    "server": "npm run dev --prefix server",
    "backend": "npm run server",
    "client": "npm run dev --prefix client",
    "frontend": "npm run client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
